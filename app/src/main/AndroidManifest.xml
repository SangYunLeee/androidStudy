<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".kotlin.outstagram.MasterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".kotlin.RetrofitActivity">

        </activity>
        <activity android:name=".kotlin.outstagram.OutstagramLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".kotlin.NetworkActivity">
        </activity>
        <activity android:name=".kotlin.RecyclerViewActivity" />
        <activity android:name=".kotlin.ListViewActivity" />
        <activity android:name=".kotlin.ContactDetailActivity" />
        <activity android:name=".kotlin.ContactActivity" />
        <activity android:name=".BrowserActivity" />
        <activity android:name=".kotlin.CalculatorActivity">
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>